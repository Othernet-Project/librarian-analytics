// Generated by CoffeeScript 1.10.0
(function(window, $) {
  var audioPlayListItemClicked, collectEvent, fileDownloadListItemClicked, fileOpenListItemClicked, galleryListItemClicked, selectors, videoPlayListItemClicked;
  selectors = {
    fileOpenListItem: 'a.file-list-link',
    fileDownloadListItem: 'a.file-list-control',
    videoPlayListItem: '.views-sidebar-video .playlist-list-item-link',
    audioPlayListItem: '.views-sidebar-audio .playlist-list-item-link',
    galleryListItem: '.gallery-list-item-link'
  };
  collectEvent = function(path, type) {
    return ($(window)).trigger('analytics-collect', [
      {
        path: path,
        type: type
      }
    ]);
  };
  fileOpenListItemClicked = function(e) {
    var el;
    el = $(this);
    if (el.data('mimetype') === 'html') {
      return collectEvent(el.attr('href'), 'html');
    } else if (el.data('type') === 'directory') {
      return collectEvent(el.attr('href'), 'folder');
    } else {
      return collectEvent(el.attr('href'), 'file');
    }
  };
  fileDownloadListItemClicked = function(e) {
    var el;
    el = $(this);
    return collectEvent(el.attr('href'), 'download');
  };
  audioPlayListItemClicked = function(e) {
    var el;
    el = $(this).parent();
    return collectEvent(el.data('direct-url'), 'audio');
  };
  videoPlayListItemClicked = function(e) {
    var el;
    el = $(this).parent();
    return collectEvent(el.data('direct-url'), 'video');
  };
  galleryListItemClicked = function(e) {
    var el;
    el = $(this).parent();
    return collectEvent(el.data('direct-url'), 'image');
  };
  $(selectors.fileOpenListItem).click(fileOpenListItemClicked);
  $(selectors.fileDownloadListItem).click(fileDownloadListItemClicked);
  $(selectors.audioPlayListItem).click(audioPlayListItemClicked);
  $(selectors.videoPlayListItem).click(videoPlayListItemClicked);
  $(selectors.galleryListItem).click(galleryListItemClicked);
})(this, this.jQuery);
